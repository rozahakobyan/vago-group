import React from 'react';

const Logo = () => {
    return (
        <div className={'logo'}>
            <img src={require('../../assets/images/logo.png')}/>
        </div>
    );
};

export default Logo;
